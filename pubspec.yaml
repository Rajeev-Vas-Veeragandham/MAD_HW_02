name: hw_02_chatboards_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔥 Firebase Packages
  firebase_core: ^2.24.2
  flutter_chat_ui: ^2.0.0
  firebase_auth: ^4.17.9
  cloud_firestore: ^4.15.8

  # (Optional but recommended UI helpers)
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/games.png
    - assets/business.png
    - assets/health.png
    - assets/study.png
